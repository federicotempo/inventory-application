<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>SteelHub - Items</title>
    <link rel="stylesheet" href="styles.css" />
  </head>
  <body>
    <header class="navbar">
      <div class="logo">
        <h1>
          <a href="/home" style="color: inherit; text-decoration: none"
            >SteelHub</a
          >
        </h1>
      </div>
      <nav class="nav-links">
        <a href="/categories">Categories 💻</a>
        <a href="/items">Items 📦</a>
        <a href="/suppliers">Suppliers 🔧</a>
      </nav>
    </header>

    <main class="content">
      <h2>Items 📦</h2>
      <div class="cards-container">
        <% items.forEach(item => { %>
        <div class="card">
          <h3><%= item.name %></h3>
          <p>
            Category: <%= categories.find(category => category.id ===
            item.category_id).name %>
          </p>
          <p>Price: $<%= item.price %></p>
          <p>Quantity: <%= item.quantity %></p>
          <p>Supplier: <%= suppliers.find(supplier => supplier.id === item.supplier_id).name %></p>
          <div class="card-buttons">
            <a href="/items/<%= item.id %>/update" class="btn update-btn"
              >Update</a
            >
            <a href="/items/<%= item.id %>/delete" class="btn delete-btn"
              >Delete</a
            >
          </div>
        </div>
        <% }); %>
      </div>
    </main>

    <footer>
      <p>&copy; 2025 SteelHub. All Rights Reserved.</p>
    </footer>
  </body>
</html>
